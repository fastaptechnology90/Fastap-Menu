class DashboardEndpoints {
  const DashboardEndpoints._();

  static const dashboard = '/dashboard';
  static const widgets = '/dashboard/widgets';
  static const metrics = '/dashboard/metrics';
  static const orders = '/dashboard/orders';
}
