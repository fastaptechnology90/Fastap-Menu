/// Shared animation and timing constants.
class AppDurations {
  const AppDurations._();

  static const splashDelay = Duration(milliseconds: 1800);
  static const pageTransition = Duration(milliseconds: 300);
  static const shortAnimation = Duration(milliseconds: 200);
  static const mediumAnimation = Duration(milliseconds: 400);
}
